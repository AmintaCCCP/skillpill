# GitHub Skill Example

This example mirrors a common GitHub skill pattern: fetching an issue from a repository and optionally returning comments or a richer response payload.

It is intentionally simple enough for contributors to understand, while still demonstrating realistic multi-parameter tool use, optional arguments, enum constraints, and clarification behavior.
